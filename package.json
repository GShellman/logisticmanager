{
  "name": "helvetic-freight",
  "version": "0.1.0",
  "private": true,
  "description": "Codex migration workspace for the Helvetic Freight logistics management game.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "validate": "npm run assets && node scripts/validate-legacy.mjs",
    "test": "node --test tests/**/*.test.mjs",
    "build": "npm run assets && node scripts/build.mjs",
    "assets": "node scripts/extract-assets.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
