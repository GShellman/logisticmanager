# Source modules

This folder is intentionally empty at the start of the migration. Codex should extract systems from the immutable legacy reference incrementally, with tests protecting existing behaviour.
